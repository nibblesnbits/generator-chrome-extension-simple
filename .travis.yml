language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-chrome-extension-simple' ]; then cd .. && eval "mv $currentfolder generator-chrome-extension-simple" && cd generator-chrome-extension-simple; fi
